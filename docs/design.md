# 设计方向

Student OS 是一个安静但有行动感的个人指挥中心。界面以浅薰衣草色作为工作底色，深色墨水文字承载信息层级，以单一靛紫色作为品牌、主操作、进度和当前导航状态的识别锚点。

布局优先服务于行动：侧边栏提供稳定的模块位置；页面标题强调当前意图；表单保持短路径录入；空状态不虚构数据，而是说明下一步可做的真实行动。文案应简洁、反思且指向行动，例如“记录一次投入，复盘一次成长”。

动画只用于必要的状态反馈，默认时长不超过 300ms，并尊重系统的减少动态效果偏好。
