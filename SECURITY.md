# 安全策略

## 支持范围

安全问题请优先针对 `main` 分支报告。项目涉及 OAuth 会话、用户数据隔离、数据库访问与 AI 网关调用，这些路径均属于高优先级范围。

## 报告漏洞

请不要在公开 Issue 中披露可能导致账户接管、跨用户数据访问、密钥泄漏或提示注入越权的问题。请通过 GitHub 仓库所有者的私密联系方式报告，并提供尽可能少但足以复现的信息，包括受影响版本、复现步骤、影响范围和可能的缓解措施。

收到报告后，维护者会确认问题、评估影响，并在修复完成后协调披露。请勿在未经同意的情况下访问、修改或导出其他用户数据。

## 安全基线

请始终使用环境变量存储密钥；不要在客户端代码中暴露服务端 AI 凭据；所有用户拥有的数据读写都应在服务端按 `userId` 校验。AI 输入必须明确区分数据和指令，且不得把不可信内容当作系统指令执行。
