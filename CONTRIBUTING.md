# 贡献指南

感谢你为 Student OS 贡献时间。项目关注可验证的个人成长数据、严格的用户数据边界和轻量但有品质的使用体验。

## 开始之前

请先阅读根目录 [README](README.md) 并完成本地环境配置。新功能开始前建议先创建 Issue，清楚说明使用场景、预期结果、数据影响与验证方式，避免在未经讨论的情况下引入大规模重构。

## 开发流程

请从最新 `main` 分支创建聚焦的功能分支。一次 Pull Request 应专注解决一个问题，并在描述中说明用户价值、主要设计决定、数据库迁移（如有）和测试结果。不要提交 `.env`、访问令牌、真实用户数据、构建输出或任何由运行环境生成的文件。

## 数据库与数据隔离

涉及领域数据的更改须同步更新 `drizzle/schema.ts`、生成迁移，并审查生成的 SQL。所有读取、更新和删除必须以当前用户的 `userId` 为边界；仅按记录 `id` 查找后直接修改是不可接受的。

## AI 功能约定

AI 功能只能分析已保存的真实数据。不要在提示词、接口或前端演示中伪造用户任务、学习记录、项目进度、评分、评价或每日结论。输出应采用可验证的结构化数据，并在缺少事实依据时明确说明限制。

## 质量检查

在提交 Pull Request 前，请运行：

```bash
pnpm check
pnpm test
pnpm build
```

同时请确认关键路径具备加载、空数据与错误状态；表单可通过键盘操作；小屏幕下没有横向溢出。

## 提交信息

建议使用简洁、说明意图的提交信息，例如：

```text
feat(tasks): add task edit dialog
fix(ai): reject empty daily activity
docs: clarify local environment setup
```

## 行为准则

参与本项目即表示同意遵守 [行为准则](CODE_OF_CONDUCT.md)。
