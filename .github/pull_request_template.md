## 变更概述

请简要说明此 PR 解决的用户问题和实现方式。

## 检查清单

- [ ] 已确认所有用户拥有的数据读写均按 `userId` 隔离。
- [ ] 如涉及 schema，已生成、审查并提交迁移文件。
- [ ] 如涉及 AI，输出仅基于保存的真实数据，且没有虚构内容。
- [ ] 已运行 `pnpm check`。
- [ ] 已运行 `pnpm test`。
- [ ] 已为关键交互提供加载、空数据和错误状态。
